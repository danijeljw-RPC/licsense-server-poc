<Project>

  <PropertyGroup>
    <Nullable>enable</Nullable>

    <ImplicitUsings>enable</ImplicitUsings>

    <!-- Generate packages.lock.json -->
    <RestorePackagesWithLockFile>true</RestorePackagesWithLockFile>

    <!-- Make compiler warnings fail the build -->
    <TreatWarningsAsErrors>true</TreatWarningsAsErrors>

    <!-- Enable modern .NET analyzers -->
    <!-- Recommended keeps warnings actionable for application/template code. -->
    <AnalysisMode>Recommended</AnalysisMode>

    <!-- Never emit .pdb files, for build or publish, in any configuration. -->
    <DebugType>None</DebugType>
    <DebugSymbols>false</DebugSymbols>
  </PropertyGroup>

</Project>
