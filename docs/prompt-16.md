# Stage 16 of 16 — Converge migrations, acceptance tests, and documentation

Read `docs/README.md` in full and treat its non-negotiable requirements, acceptance checklist, and definition of done as the final authority. This is the last stage after prompts 01–15. Inspect the entire working tree and all prior-stage reports/code. Preserve unrelated user work. This stage is for completing and verifying the upgrade, not producing a gap-analysis-only response.

Create a traceability matrix from every roadmap acceptance item to its production implementation and automated test. Run the full unit, integration, authorization, concurrency, browser, signing/validator, container, and security-negative suites. Exercise UI and bearer API parity for issuance/lifecycle/catalog/user actions; 1,000 concurrent ID allocation; one-time secrets; email metadata; expiry/signing; role matrix; API idempotency/concurrency; outbox retry; customer magic links; and Stripe duplicate/reordered/malicious webhooks. Fix all failures and missing coverage within the roadmap scope.

Review security boundaries end to end: no plaintext activation codes/API keys/magic tokens/payment secrets in persistence, logs, audit, URLs, exceptions, analytics, or later responses; cookie mutations use antiforgery; bearer authentication uses explicit headers; authorization is action-level; rate limits are appropriate; offline limitations are accurate; all sensitive mutations are transactional, versioned where needed, and audited with correlation IDs.

Finalize EF Core migrations. The roadmap authorizes removal of this repository's disposable Compose containers and volumes and allows one migration re-baseline because the PoC history is unestablished. Before deleting anything, resolve and verify the exact Compose project/resources. Re-baseline only if the resulting model is finalized and no non-disposable/published database depends on the current migration; otherwise keep forward migrations. Prove clean PostgreSQL creation, restart, repeated seed, upgrade path if retained, and app readiness. Never target unrelated Docker resources.

Update root documentation, `.env.example`, configuration validation, OpenAPI, operator setup, recovery/runbooks, and the roadmap status so commands and behavior match reality. Document secrets by variable name only, MailerSend/Stripe setup, roles/scopes, backup/retention concerns, the KMS/HSM production boundary, and known fundamental offline limitations. Remove obsolete no-op/development-only paths that could activate in production and eliminate stale free-text/admin-role behavior.

Run the documented commands from a clean checkout-equivalent state, including Release build, all repository test scripts, Docker image build, Compose readiness, and a final smoke flow. Do not declare completion with skipped critical tests or unexplained failures. Finish with the traceability result, migration strategy, exact commands/outcomes, any environment-only tests not run and why, and remaining risks that are genuinely outside this roadmap.
