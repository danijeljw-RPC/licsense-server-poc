# Stage 12 of 16 — Add durable transactional email and MailerSend

Read `docs/README.md` in full. This is stage 12. Inspect the no-op Identity sender, issuance/user flows, database transactions, hosted-service patterns, configuration, tests, and working tree. Preserve unrelated changes. Because provider APIs/packages are time-sensitive, consult current official MailerSend and Microsoft documentation before selecting integration details; cite the relevant official sources in the completion report.

Introduce provider-neutral `ITransactionalEmailSender`, a durable PostgreSQL `EmailOutbox`, template name/version mapping, and a MailerSend adapter. Cover templates for purchase/activation delivery, renewal reminder/receipt, payment failure, invoice, operator invitation, Identity email confirmation/recovery, and customer magic links. Replace the no-op Identity sender through this abstraction.

Write business state and its outbox row in the same database transaction. Send asynchronously with a hosted worker, bounded exponential-backoff retries, lease/lock handling safe for multiple app instances, and idempotency keys so crashes or worker retries do not duplicate delivery. Persist provider message ID, attempt count, next attempt, and final status for operational visibility. Do not hold database transactions open across network calls.

Keep MailerSend API tokens, sender identity/domain, and webhook secrets outside source control. Validate configuration with safe development behavior and fail securely in production. Redact addresses/content appropriately in logs, enforce retention, and do not copy full message bodies or tokens into audit. Do not email activation-code plaintext except through an explicitly approved issuance template with clear one-time-secret handling.

Add an authenticated MailerSend delivery-status webhook for delivery, bounce, and complaint updates. Verify authenticity using the provider's current documented mechanism before parsing/mutating, deduplicate provider event IDs, update operational status idempotently, and never trust it to change license state. Add a development/test transport, deterministic worker tests, and adapter contract/webhook tests without making live network calls. Cover transaction rollback, concurrent workers, crash recovery, retry schedule, permanent failure, duplicate suppression, redaction, Identity integration, invalid webhook authenticity, and reordered/duplicate delivery events.

Add migration, run targeted/full tests and clean migration/seed checks, then smoke-test queued delivery with the fake transport. Finish with configuration names (never values), operational behavior, official-source links, and exact commands/results.
