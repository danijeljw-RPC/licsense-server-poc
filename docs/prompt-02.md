# Stage 02 of 16 — Implement lifecycle, expiry, and signed-expiry invariants

Read `docs/README.md` in full. This is stage 02 and assumes stage 01's tests/support are present. Inspect the actual code and working tree before editing; prior-stage code is authoritative where it has advanced. Preserve unrelated changes. Implement and verify the feature—do not stop at analysis.

Build the shared domain/service layer for license terms and lifecycle. Add cancellation persistence (`CancelledAt`, reason, actor/reference) separately from revocation and implement the required state precedence: cancelled, revoked, expired, active, available. Cancel only a license with no activation history; make cancellation and revocation terminal and idempotent under their documented rules. Block activate, validate, and refresh for cancelled, revoked, or expired licenses.

Create controlled server-side license-type validation for exactly `perpetual`, `subscription`, and `evaluation`. Subscription/evaluation require a future expiry at issuance. Perpetual always stores the canonical instant `9999-12-31T23:59:59.999999Z`; do not use null as its canonical domain value. Add an authorized terms-amendment operation for expiry, seats, and updates-until. It must permit an explicit past expiry, require a reason, increment/check `Version`, and audit old/new non-secret values, actor, timestamp, result, target, and correlation ID. Edition amendment may remain unavailable until the stage-07 allowlist exists; do not accept new free-text edition values as an interim shortcut.

Make issuance and lifecycle mutations transactional and concurrency-safe. Consolidate rules so UI and later APIs call the same services rather than duplicating state logic. Ensure the signed entitlement always contains `expiresAt` equal to the canonical server record for every license type. Preserve the signed-schema entitlement array for future multi-product tooling.

Add the EF Core model configuration, PostgreSQL constraints/indexes that reasonably reinforce these invariants, and a migration. Keep audit append-only through application services and EF/database safeguards against normal updates/deletes. Update seed data so it remains valid. Do not expose or log activation secrets while improving audit context. Repeat the offline-file limitation in service/API-facing descriptions where relevant: online checks observe changes immediately, previously downloaded offline files cannot be recalled.

Make the relevant stage-01 lifecycle, expiry, signed-expiry, concurrency, and activation tests green. Run targeted tests, the full .NET test suite, and a clean temporary-PostgreSQL migration/seed check. Finish with changed behavior, migration name, commands/results, and any remaining failures that belong to later stages.
